// Mr. BadHabits — Innate: Arcane Aura
// A DEDICATED copy of Crystal Maiden's Arcane Aura so BadHabits gets a proper INNATE without
// editing the shared crystal_maiden_brilliance_aura definition (that ability belongs to a real
// hero and must stay untouched). BaseClass inherits the built-in aura's logic; the three innate
// keys (Innate/INNATE behavior/MaxLevel 1) put it in the dedicated innate slot instead of a
// learnable ability slot -- which was the bug (it showed as a normal ability).
"DOTAAbilities"
{
	"mr_badhabits_arcane_aura"
	{
		"BaseClass"				"crystal_maiden_brilliance_aura"	// inherit the mana-regen aura
		"AbilityBehavior"		"DOTA_ABILITY_BEHAVIOR_PASSIVE | DOTA_ABILITY_BEHAVIOR_INNATE"
		"AbilityType"			"ABILITY_TYPE_BASIC"	// NOT ATTRIBUTES: ATTRIBUTES counts the innate as a 9th talent and overflows the talent UI (UpgradeName9 crash). Innate slot comes from "Innate" "1".
		"AbilityTextureName"	"crystal_maiden_brilliance_aura"
		"MaxLevel"				"1"
		"Innate"				"1"		// show in the dedicated innate slot
		"AbilityUnitTargetTeam"	"DOTA_UNIT_TARGET_TEAM_FRIENDLY"
		"SpellImmunityType"		"SPELL_IMMUNITY_ALLIES_YES"

		// Single-level innate values (the shared CM version is 4-level; an innate is MaxLevel 1).
		"AbilityValues"
		{
			"base_mana_regen"				"0.8"		// flat mana regen to allies in the aura
			"proximity_mana_regen_tooltip"	"2.4"		// stronger regen when near BadHabits
			"proximity_bonus_factor"		"3"
			"proximity_bonus_radius"
			{
				"value"						"1200"
				"affected_by_aoe_increase"	"1"
			}
		}
	}
}
